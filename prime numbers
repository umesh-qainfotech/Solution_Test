//"Return a list of all prime numbers between the given range (both ends not included)
lower = int(input())
upper = int(input())

list = []

for num in range(lower, upper + 1):
   
   if num > 1:
       for i in range(2, num):
           if (num % i) == 0:
               break
       else:
           list.append(num)
print({"list":list})           
